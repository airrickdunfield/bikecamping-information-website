# Project Memory

- Keep the local Vite dev server running during an active work session after starting it for browser checks.
- The in-app browser is expected to use `http://127.0.0.1:5173/` while working locally.
- If the browser says the site cannot be reached, check and restart `npm run dev -- --host 127.0.0.1` before treating it as a code issue.
